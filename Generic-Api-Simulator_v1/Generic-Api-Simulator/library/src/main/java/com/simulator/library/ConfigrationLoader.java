/*
package com.simulator.application.config;

import java.io.File;

import com.simulator.pojo.Config;
import org.springframework.stereotype.Service;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import com.fasterxml.jackson.databind.ObjectMapper;

import javax.annotation.PostConstruct;

@Service
public class ConfigrationLoader {

	Config config;

	public Config getConfig() {
		return config;
	}

	@PostConstruct
	public void loadConfig(){
		// throw exception if config file is not found and log the message.
		//schemaFile
		// validate config with schema. Throw exception if not valid and log the reason for exception.
		// load config object (POJO)
	}
}*/
